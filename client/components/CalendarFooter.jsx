import React from 'react';

const CalendarFooter = (props) => (
  <div className="footer">
    <button type="submit">
      <u>Clear dates</u>
    </button>
  </div>
);

export default CalendarFooter;
